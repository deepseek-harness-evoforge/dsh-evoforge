{
  "name": "dsh-evoforge",
  "version": "0.0.0",
  "private": true,
  "description": "Evidence-driven, cache-stable extensions for DeepSeek Harness",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deepseek-harness-evoforge/dsh-evoforge.git"
  },
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "generate:typert": "pnpm --filter dsh-evolve exec tsc -b ../../tsconfig.host.json --force && node scripts/generate-typert.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "test:pa1": "pnpm --filter dsh-evolve exec vitest run test/auto-promotion-policy.test.ts test/candidate-impact.test.ts test/sealed-trial-darwin.e2e.test.ts test/shadow-resume.e2e.test.ts test/feedback-shadow-launcher.test.ts test/generation-store.e2e.test.ts test/evaluator-draft-inbox.test.ts test/evaluator-authoring-crash.e2e.test.ts test/retention-gate.e2e.test.ts && pnpm --filter dsh-software-delivery exec vitest run test/verify-delivery.test.ts test/complete-delivery.test.ts && pnpm --filter dsh-evolve-web exec vitest run test/evolution-action.client.test.tsx && pnpm --filter dsh-telegram exec vitest run test/protected-route.test.ts test/telegram-api.test.ts test/inbound.test.ts",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm run check:docs && pnpm run typecheck && pnpm run test && pnpm run build"
  }
}
