# Web 原生单次审批与恢复验收

## 执行前固定的范围

本页在发送任务前创建。生产起点 `150dba0`，DSH `0.1.6-alpha.1 / 0d1f500`，保持单一Host、现有凭据、
渠道绑定、工作区及演化额度。使用同一工作区的一个新原生会话，只将这个会话设为“仅可查看”，审批策略ask。
既有飞书会话、客服验收会话和旧拒绝验收会话不修改权限、不重跑任务。

任务A2：只通过原生bash执行以下命令，创建一个明确范围的验收文件：

```sh
printf '%s\n' 'approval-once-20260918' > <工作区中的仓库绝对路径>/.evoforge/real-acceptance/2026-09-18-approval-once/approved.txt
```

公开记录将本机绝对路径脱敏为占位符；实际发送前已展开并固定为该仓库路径，执行中未更换。
发送前确认目标不存在、父目录存在。仅在实际沙箱拒绝后申请一次workspace-write审批，不改命令或路径。
操作者先刷新整个页面，再在恢复的原生请求上选择“允许一次”。不得联网、外发、读取其他文件、调用Skill、Goal或子代理。
成功后停止，不自行补做其他操作；若拒绝或取消则如实结束，不换工具绕过。

固定检查：

1. 未决定前文件不存在，native asked尚无decided；页面能看到具体命令与待审批选择。
2. 整页刷新后同一请求仍可决定，不另发任务或创建第二个请求。
3. 一次允许对应一次执行；文件精确为上述一行加换行，standing权限仍为read-only，而非永久workspace-write。
4. completed结果与真实文件一致；不是仅凭模型声称成功。
5. 完成后冷启唯一Host，既有Session前缀、审批配对和文件内容不变，无自动重放或新外发。

这验证原生交互协议，不是独立任务正确率、学习收益或飞书审批证明。
此前已确认的“待审批仍显示深度求索中”原生问题保持单独记录，不以新插件、DOM改写或放宽权限掩盖。

## 实际结果（2026-09-18，真实 Web / 真实模型）

会话 `session-40bada67-97dd-4760-a07a-6ef5e4bd1544`，模型 `gpt-5.6-sol`、标准模式。
未修改运行代码或包，生产仍为上述版本；本次是审批路径验收，不是新功能修复。

- 首次bash确实返回 `Operation not permitted` / read-only sandbox拒绝；第二次请求同一命令的workspace-write升级。
  原生两次tool/call的命令相同，printf格式中的反斜杠经字符码核对为单个92，随后110（n）。
- 待决定时整页刷新：原命令及“拒绝 / 允许一次”恢复。原生仅一个asked，
  ID `5054f426-05c3-47b2-9874-d250c4a14a25`，无decided，目标文件仍不存在。
- 点击一次“允许一次”，同ID记录 `allowed-once`；总共两次bash（首次拒绝、第二次执行），没有第三次调用。
  最终原生turn/end为completed，页面报告已创建；实际文件精确23字节、内容为固定标记加换行，
  SHA-256 `2485aca2292905349ce35bf5b3cac2f5776494cc1dcab3a08a335980ff686df1`。
- 会话最后的permission/preset和sandbox/mode仍为read-only；页面仍显示“仅可查看”。
  全局profile、既有会话权限没有为此放宽。
- 后台纠正检查已为inspection-finished后才停止PID99343；确认端口释放，再冷启唯一Host PID572。
  整页刷新后原结果、1轮3步、仅可查看权限恢复；无新任务提交。
- 使用官方JSONL SessionPersistence、只读open/read比较：任务前30个原生Session的header与事件全部一致，追加0；
  重启前31个Session全部旧前缀一致，只追加一条session/end-seed。审批仍为一个asked/一个decided，
  工具调用与完成轮次未增加。文件字节及mtime均未变，没有重新写入。
- 三个学习账本、两个外发账本及profile策略逐字节不变；没有新演化额度、草稿、对照或渠道外发。

任务耗时201.375秒，其中等待人工决定183.071秒；扣除审批等待为18.304秒，不能把人工检查时间算作模型延迟。
三次执行模型响应的原生totalTokens合计22,058（包含provider报告的缓存token），另有一次原生自动标题请求，
未获得其独立usage。金额未知，不把未知成本记作零，也不将本例当独立任务效果样本。

复现方式：按上方固定任务，在新只读会话发送一次 → 等黄色原生审批卡 → 核对目标不存在 → 整页刷新 →
核对同一命令 → 允许一次 → 对照文件内容、最后权限与完成结果 → 空闲后冷启 → 原会话读回并核对文件mtime。
本轮已在操作中展示待审批、刷新后审批、完成、冷恢复四幅截图。私有备份保留于
本机部署目录的 `approval-once-backup-20260918.B2c7zB`，不提交Session内容。

## 限制

本例通过“待审批刷新 + 允许一次 + 完成后冷恢复”；没有验证待审批时重启Host、飞书审批或所有权限组合。
原生“深度求索中”与黄色等待审批并存的问题再次复现，仍未关闭；没有改动DSH核心或覆盖其UI。
真实学习收益、未来会话启用/精确回滚和同条件Hermes比较仍未证明。
