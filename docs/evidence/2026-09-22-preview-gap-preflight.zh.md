# 2026-09-22：文件预览学习缺口的前置验证

本页在下面两次任务的首个模型请求前固定。它不是旧宽表格草稿或旧四题的重评、重跑或启用授权。
旧字面实验 `no-improvement`、记录核对三组均4/4及两个未启用草稿全部保留。

## 问题与范围

已有真实纠正见[多材料报告交付](2026-09-17-feishu-conflicting-material-task.zh.md)：事实正确的五列表格在飞书附件
预览中溢出，两次返工后逐项小节可读。现在先检查这个缺口是否仍出现在全新普通会话；不先生成另一份 Skill。
两份新材料是声明为虚构的普通交接文件，不冒充实际业务数据；不是给定答案的 JSON 计数题。

同一唯一生产 DSH `0d1f500 / 0.1.6-alpha.1`、现有 `gpt/gpt-5.6-sol` 与凭据、原 harness Workspace。
P1、P2 各新建一个无原纠正历史的标准原生 Session，使用原生文件与 present 工具；不手动附加草稿或系统提示。
每题只请求一次、不补充排版提示、不改产物；遇到失败仍完成另一题，全部报告。超过600秒未结束时取消并记不完整，
不自动重试。请求的其余原生默认参数不调整，实耗另记；这不是完整 Hermes 同条件试验。

当前飞书附件预览窗口截图为1079×637像素；保留窗口尺寸、缩放与字体，不为制造溢出临时缩窄。
先由新 Web 会话生成并原生展示文件，再由原已授权 DSH 私聊只传递固定文件，不能改写内容。
这次文件传递单独记请求和费用，不算生成收益；只发至现有测试私聊，不新增收件人或变更渠道绑定。

## 固定任务

两题使用同一个请求模板，只替换 P1/P2 和目录：

> PREVIEW-GAP-20260922-P1：请读取 <Workspace>/.evoforge/real-acceptance/2026-09-22-preview-gap/p1/ 下的 source-a.md 和 source-b.md，整理一份供同事在飞书附件预览中阅读的事项交接报告。逐项写清负责人、截止、当前状态与依据，保留未解决冲突和未知信息，将尚未决定的建议与已确认事项分开。写为同目录 report.md，写后完整回读核对，再用 present 交付到当前 Web。我之后会把原文件发到测试私聊。仅允许读取这两份输入和自己的结果、写入并展示这个新结果；不改输入或其他文件，不用命令、联网、外发、Goal、子代理或提权。不要声称已经检查了飞书实际预览。

P2 模板中的标记改为 P2、目录改为 p2；其余逐字相同。`<Workspace>` 只替换为原 harness 工作目录绝对路径。
任务不规定表格或小节，也不泄漏既有 Skill。四份输入裸字节 SHA-256（P1 A/B，P2 A/B）依次为：

- `b2c36f59d3071902e79187f25d0e09c08a78cb8d5d4faf6892f6d0b524ead8db`。
- `4aa855c8865c234e41a24a4f5053c9f5112945e07f55b76752a3f170deb6c46e`。
- `e322f32869344bb8b49a2800af2c1b2ec561b86606d4eb00f5ae1217ec66fe40`。
- `157aa19703bd06c40d2e1a0afbe2c0004a90497682c76f3b1f0a3377c694fbc9`。

P1 source-a.md：

```md
# 展厅开放日执行记录 A（周一）
- 导览页：梁负责，周四截止，待完成。
- 签到台：唐负责，周五截止，待完成。
- 音响检查：冯负责，截止未定。
```

P1 source-b.md：

```md
# 展厅开放日补充记录 B（周二）
- 签到台：已确认改由邵负责，仍为周五，已完成；取代 A 中的原负责人和状态。
- 导览页：梁仍说周四；协调人提出周六，但两人尚未确认，不能视为截止已改。
- 音响检查：冯继续负责，完成状态未提供。
- 仅讨论：增加临时寄存处，未决定实施，也未指派人员。
```

P2 source-a.md：

```md
# 内部培训交接记录 A（周三）
- 练习材料：陶负责，下周一截止，整理中。
- 演示账号：邢负责，本周五截止，未开始。
- 录像整理：俞负责，截止未定，尚未完成。
```

P2 source-b.md：

```md
# 内部培训确认记录 B（周四）
- 演示账号：已确认仍由邢负责，改到下周二，现已完成。
- 练习材料：陶提出下周三，讲师仍要求下周一，截止争议未解决；整理工作仍在进行。
- 录像整理：已确认改由鲍负责，截止仍未定；没有提供新的完成状态。
- 有人建议开放外部报名，但尚未决定，不能列为已承诺执行事项。
```

## 在生成之前固定的验收

两题均需完整读两输入、真实写出并完整回读一个结果、原生展示成功、输入未变、无越界；不以最后一句“已完成”代替工具证据。
逐项人工核对事实与依据，允许自然语言等义表述、字段顺序及任意可读版式，不按参考句逐字匹配，不以出现表格直接判失败。

| 题 | 必须保留的事实 |
| --- | --- |
| P1 | 恰好三项已确认事项；导览梁/周四与周六未决/待完成；签到邵/周五/已完成；音响冯/截止未定/状态未知；寄存处仅建议；变化和冲突可追溯A/B |
| P2 | 恰好三项已确认事项；练习陶/下周一与下周三未决/整理中；账号邢/下周二/已完成；录像鲍/截止未定/仍未完成；外部报名仅建议；变化和冲突可追溯A/B |

视觉验收用同一个实际飞书预览器，检查全文：上述必要字段与依据无需横向滚动即可读，不被裁切或遮挡；正常纵向滚动允许。
下载件须与 Web 产生的原文件 hash 一致。不点击正文中自动识别的外部链接。
事实通过、实际交付通过和视觉通过分别报告；端到端通过要求三者都通过。窗口变化、无法预览或传输未知不能记视觉通过。

只有两次独立新会话都复现视觉问题，才认为这个窄工作流值得进入后续学习研究；一题失败只说明单次失败，两题通过则无新增缺口。
这个2/2是本次前置研究判据，不是用户费用限制，也不授予任何 Skill 启用资格。
无论结果如何均不改题、不追加同族抽样来凑失败，不把这两题再称为后续未见评测。
后续若开展学习，须另外在起草前封存未见适用及保留任务，并保持提出者隔离；不能追补旧草稿的启用资格。

## 实际结果

本节创建时尚未运行。后续仅追加观测和限制。
