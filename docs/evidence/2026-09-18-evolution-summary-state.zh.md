# 演化概览状态与实际能力边界

2026-09-18。真实页面基线 `c3a1d20`；DSH alpha.1/0d1f500，原生控制台→演化。
顶部显示「暂无待处理事项」，但同页有一份未验证草稿、一个未完成实验，以及一次未观察到改善的结果。
读取尚未完成时也会显示同一健康状态。普通草稿部分仍沿用「你决定是否给未来 Session 使用」步骤，
实际没有这个草稿的启用入口。此处属于 EvoForge 自有视图，不是前一轮的 DSH 原生审批问题。

## 改动边界

用户结果：概览明确区分当前读取与改进状态，普通聊天可以继续，但不能将暂无授权动作看作改进验证完成。
只消费既有 overview Remote；不增加模型、工具、事件、持久化、预算、审批、启用或回滚权限。
当前 Session/Workspace 切换、last-good 保留及合法 review 操作保持原样；卸载/回退只移除或恢复展示代码。
先有授权操作时仍显示待确认数；否则分别显示读取中、状态未确认、检查中、改进尚未验证或暂无待处理事项。
普通草稿只显示当前确实存在的流程边界，不创造不可执行按钮，不自动重跑或改判历史实验。

## 本地验证

修改前五个目标断言失败，包含四种实验结论（即使单组观察到改善也不能声称完整改善已验证）与初始读取状态。
修改后 Web 两文件 47 项通过，typecheck 通过；另覆盖中英文、运行中→闲置→刷新失败，以及不新增待确认动作。
初次新增 fixture 显式给 optional 字段 undefined，typecheck 拒绝；已改为合法空闲快照并重新通过。
这些测试不证明真实 Skill 收益。生产只计划更新 dsh-evolve-web 包；部署/真实页面结果在完成后追加。

## 部署与实际页面

修复提交 `7c06232` 已推送。原生包合同 24 项通过；完整打包的干净 profile 两项生命周期检查通过（45.12 秒），
覆盖官方安装、dump、启动、原生 Session/Goal/Storage、释放、移除与原生历史回读。
核心仍为已审计的 `0d1f500`，本轮 fetch 后无源码改动，沿用当日 frozen install/完整 build 证据，不重新构建运行中的核心。

先确认原生后台任务 inspection-finished，再备份、停止旧 Host 88390、确认已退出，官方 plugin add 只替换
dsh-evolve-web；control pack 为 `67215e8ce1b2506fba06e9b84384375f4aea76640792e13b16330300bdfab03d`。
新唯一 Host PID 92223，仅监听 127.0.0.1:3000。安装 client.js SHA-256 为
`351e489cbb660387e1e9355c7236ad4cc4406c4db2ac06ebbab8141761222e1e`，与测试/打包产物相同。
profile 依赖差异只有 Web 包一个路径，Bundle 选择和策略文件不变；dump 中仍只有一个 Web row。

安装命令成功，但裸 `pnpm peers check` 仍报 profile 缺少由 DSH Host 提供的 peer，不能说该命令通过。
没有为消除警告再安装第二份核心；实际官方安装/启动、完整 clean-profile 生命周期与当前浏览器加载均成功。

生产整页重载保留原控制台→演化选择；真实截图中标题旁为琥珀色「改进尚未验证」，不再绿色显示暂无待处理事项。
旧未完成实验及双方字面 0/4 保持可见；第三步改为「普通草稿暂无启用入口」，并说明照常聊天、隔离检查、
尚需未见任务收益和发布资格。再次点击刷新后五个页面断言通过：新状态、旧失败、旧分数、新能力边界均存在，
错误的空闲状态不存在。未点击晋升、重试或调整预算。新 loading/error 分支有组件证据，本轮未制造新的网络中断。

部署备份 `draft-status-backup-20260918.9gviAD`：十个插件账本和策略文件字节不变；44 个 Session 文件中 43 个字节
不变，当前验收会话仅追加一个原生 end-seed，官方 reader 验证旧 header 和全部事件前缀相同。
没有新增模型调用、外发消息、实验或生成版本；旧包保留，可只回退 Web 展示，不回写历史。
这修复了状态误导，不代表可靠自我进化、普通草稿启用/回滚或 Hermes 比较已完成。
