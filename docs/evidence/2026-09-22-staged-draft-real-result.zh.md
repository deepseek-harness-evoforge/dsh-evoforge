# 分阶段准备的真实结果：请求恢复完成，校准失败，未成稿

## 结果与范围

2026-09-22，生产源码 `64828bb`，前置传输修复 `8d970b6`。仍使用同一普通回答负反馈：
客服导出文件实际14行，回答误报15行；原来源、反馈及其余正确统计未改。
来源及首次失败见[明确回答反馈接入](2026-09-18-explicit-feedback-intake.zh.md)。

本次八次准备请求全部返回响应，未再在内容等待60秒处终止；最后一步未通过治理校验，
结果为 `abstained / invalid-governance`。只保存了四份任务和前三份校准材料。
没有调用 Skill 提出者，没有新草稿、独立对照、启用或回滚结果，也没有 Hermes 比较。
这只证明这一次分阶段请求完成了传输，不证明长期可靠，更不证明学习收益。

## 不改题的失败记录

原失败链全部保留。本次新记录
`f482b72a757cae92ad35321d965c1a3190ffc8c8738060d668a0280b76b0142c`
绑定直接父记录
`f65aa155971a1b128e8239589eb4aa2de3b28eb52b3d51539c1f6ca2c28a5cc4`，
协议为 `staged-v1`。预留9次，实际发送8次；没有消耗第9个提出者请求。
没有读取保护材料来手工撰写 Skill，没有重抽最后一道题，也没有把本次失败改成可重试传输错误。

| 准备请求 | 总耗时（毫秒） | 首片段（毫秒） | 结果 |
| --- | --- | --- | --- |
| 固定任务1 | 39174 | 25271 | 保存 |
| 固定任务2 | 33576 | 24931 | 保存 |
| 固定任务3 | 30935 | 17517 | 保存 |
| 固定任务4 | 32294 | 21129 | 保存 |
| 固定材料校准1 | 46202 | 34344 | 保存 |
| 固定材料校准2 | 36334 | 26456 | 保存 |
| 固定材料校准3 | 50278 | 39677 | 保存 |
| 固定材料校准4 | 28220 | 21473 | 校验失败 |

八次合计297013毫秒；记录到输入6221、输出7427 token，共13648 token，费用未知。
总耗时为请求时间之和，不含操作等待。原生 Jobs 刷新后显示 `draft-incomplete`、4分57秒。
失败响应正文未落盘，当前记录不能区分最后一次 JSON/schema、字面正反例校准或完整材料组装中的具体失败。
因此不能把它断言为某一条断言、模型事实或某个上游服务错误。

## 调整前的受控诊断

均通过同一个 DSH 原生 LLM 服务和已有凭据，未导入诊断题到本次生产治理材料：

- 移除插件内容空闲计时后，三次完整治理请求仍在61–64秒失败。临时原生观察器捕获 `TRANSPORT` 和连接信号，
  没有 HTTP 状态；不能据此认定服务端存在60秒时限。SDK报告0/0不证明免费。
- 三个短确认参数对照均成功：原来源会话与4000输出上限、同来源不指定上限、无来源会话与4000上限。
  分别3812、11974、1534毫秒，每次输入23/输出5 token。不能以此证明复杂治理请求可用。
- 将完整约束从四题减至一题仍失败：61583毫秒、`TRANSPORT`。所以并未只把四题改成四次同样复杂的请求。
- 相同源输入的短确认4985毫秒、只产任务与参考答案20146毫秒、对该固定任务独立校准17792毫秒成功。
  这三次共记录3080 token；支持尝试拆开请求职责，不是上游根因证明或学习证据。

所有临时诊断 Host 均已退出；生产启动参数没有诊断 patch。早期凭据服务尚未就绪的探针拒绝不计为有效 Provider 对照。

## 已部署改动及受控验证

DSH 仍为已审计支持版本 `0.1.6-alpha.1 / 0d1f50007f9bca3f52b06e1c3074fa14d5fb0720`，未改核心。
本轮 canonical fetch 的 `origin/master` 为 `ddefc45`；没有把支持版本称为最新。
支持源码的 frozen install 和完整 build 通过。

`8d970b6` 将传输活性判断交回原生适配器，保留600秒总 deadline，保存无正文请求时序；
`64828bb` 将任务与校准分阶段追加到原有 DSH Domain，提出者仍只能看到原纠正。
传输失败恢复只继承已保存前缀；校准失败不自动重抽。每次发送都有独立持久标记，旧调用不被隐去。
详见[插件契约2.4](../plugin-contract.zh.md#24-普通纠正的隔离-skill-草稿)。

实际执行的验证包括：

- 改动前新阶段测试为红；最终草稿、原生反馈、原生纠正、配置、评测存储/guard及声明共7文件95项通过，测试类型检查通过。
  命令使用 `DSH_EVOLVE_DSH_SOURCE_DIR`、`NODE_OPTIONS=--experimental-transform-types` 与 `vitest run --maxWorkers 1`。
- Web `evolution-action.client.test.tsx` 45项及测试类型检查通过；未把同命令中不存在的另一个文件计为已运行。
- 官方类型生成、Host/Web构建、docs/CI/suite合同15项及差异检查通过。
- `dsh-software-delivery` 的 `clean-profile-suite.e2e.test.ts` 两项通过（36.16秒），实际12包安装、dump、
  原生 Session/Goal/Storage、dispose/remove、卸载后读取。上述受控模型 fixture 不算真实改善。

仅官方 plugin add 更新演化 Host/Web 两个 tarball；安装后文件与已测构建逐字节一致，dump通过。
Host tarball SHA-256：`1e663449bffe0b2882179b23848cf6cc172b63e6b4e8158d60809ee14fb80775`；
Web：`8f3d7a3f3dff0ab6b5b878d784652a6670438e31b32901ec3fe5c369428d4136`。
最终只有生产PID33460监听 `127.0.0.1:3000`，旧Host退出后才启动新Host。

## 历史、权限与剩余缺口

最终只读核对部署前备份：原Session目录50个物理文件逐字节不变；10个非草稿插件账本逐字节不变，
包括渠道入站、文本/文件出站、Generation和旧评测；原7条草稿记录完全相同，只新增上述失败记录。
没有新飞书消息、原生任务会话或 Generation 选择。原模型、凭据、权限和路由保留。
配置中的调用数是部署执行值，不是用户提出的费用限制；用户已授权为目标使用模型API。

当前路线的实际缺口不是继续加等待时间：起草依赖模型写出多组字面校准，而现有执行器只准 `skill`，
不能读写被测工作文件。因此即使文本校准通过，也不能验证这次原错误所代表的文件统计/交付工作流。
后续应以预先冻结的真实文件输入、独立可执行验收和实际产物对照验证方法，不重评或覆盖本次失败。
真实未见任务改善、跨任务不退化、启用/回滚和同条件 Hermes 仍未完成。
