# Web 固定文件任务、刷新与产物打开实测

日期：2026-09-15（Asia/Shanghai）。本次是现用单一 Host 的真实 Web / 模型任务，非 fixture。

## 环境与边界

- EvoForge 源码基线：`bb8cfc62fa11d8a6415cb6dbd6bf2edd2b63a8a5`。
- 页面版本标记：DSH 本地构建 `0.1.2-alpha.5+db6dc3`；不是待升级的 rc2。
- 浏览器：Codex 内置浏览器，`http://127.0.0.1:3000/`；原有“独立重连验收准备”会话。
- 模型：页面原有 `gpt-5.6-sol`；权限：工作区内修改。
- 验收后 `lsof -nP -iTCP:3000 -sTCP:LISTEN` 仍只有原 Node PID 40511 监听本地 3000 端口。
- 没有升级、重启 Host，没有修改凭据、授权、插件配置或业务文件，没有创建子智能体。

## 固定任务与复现步骤

1. 打开现有验收会话，确认 9 月 13 日的“准备完成”回复仍可见。
2. 发送以下固定任务（不可重复覆盖产物）：

   > 固定验收任务 WEB-20260915-A：仅在当前工作区执行，不读取其他目录或凭据，不联网，不创建子智能体。先检查 qa-web-20260915-a.txt 是否存在；若存在立即停止，不覆盖。若不存在，创建该文件，内容严格为两行：第一行 apples=3，第二行 oranges=4，末尾换行。随后重新读取此文件验证，回复文件绝对路径、读回内容及 total=7。必须真正执行文件操作，遇到权限或工具失败请明确报告失败，不得声称完成。

3. 观察侧栏“进行中”、正文执行提示与“停止生成”按钮；等待完成。
4. 在本机独立读取目标文件，逐字节比较 `apples=3\noranges=4\n`。
5. 刷新页面，确认同一会话中的原任务、4 次工具调用入口、结果和产物入口仍在。
6. 点击回复中的绝对路径。页面本身没有改变，但系统“文本编辑”打开了对应文件；确认窗口名和读回内容。

## 结果

- 任务成功：验收工作区内的真实文件 `qa-web-20260915-a.txt` 为 19 字节，内容完全匹配，回复 `total=7`。本机绝对路径不写入公开记录。
- 页面显示本轮 4 次工具调用、用时 21 秒、用量 29.6K token。这是 UI 显示值，不是独立计费账单，也没有价格数据。
- 执行期间没有人工审批或纠正；提交后的任务级人工干预为 0。
- 刷新后结果恢复；未重新发送任务，未产生第二份产物。
- 产物入口通过原生系统应用打开正确文件，文本编辑器可见两行内容。
- 窄窗口初次显示展开侧栏，刷新后变为收起侧栏；本次记录为布局一致性待观察项，尚未确定原因或判为缺陷。

## 限制与下一步

单个简单任务只能证明这一成功路径。没有证明失败、审批、断网、Host 重启、飞书、未见任务效果、自我进化或 Hermes 对比；也不证明 rc2 已部署或可升级。下一步继续真实飞书路径及失败/授权反馈验收，再推进受控升级；不能用此前兼容性测试替代真实使用验收。
