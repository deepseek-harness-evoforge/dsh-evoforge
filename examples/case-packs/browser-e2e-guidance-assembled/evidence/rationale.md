# Observed failure

A GUI plugin change passed component tests, but its real browser flow was never exercised. The user could not tell whether refreshed host state or the visible failure path worked.
